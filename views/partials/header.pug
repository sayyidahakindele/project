head
	style
		include ../css/navbar.css
body
	div#header
		a#link(href="/") Home 
		b
		if (session.loggedin)
			a#llink(href="/logout") Log Out
			b
			if (session.type == 1)
				a#llink(href="/adminprofile") ADMIN
				b
				a#llink TRAINERS
				b
				a#llink MEMBERS
				b
				a#llink(href="/billingandpayments") BILLING AND PAYMENTS
				b
			else if (session.type == 2)
				a#llink(href="/members/" + session.user_id) Hi #{session.username}!
				b
			else if (session.type == 3)
				a#llink(href="/trainers/" + session.user_id) Hi #{session.username}!
				b
		else
			a#llink(href="/login") Log In
			b